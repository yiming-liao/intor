// Init translator options
export type InitTranslatorOptions = {
  loadingMessage?: string;
  placeholder?: string;
};

// Resolved translator options
export type ResolvedTranslatorOptions = {
  loadingMessage: string;
  placeholder?: string;
};
